
Процедура ПередЗаписью(Отказ, РежимЗаписи, РежимПроведения)
	
	Если НЕ ЗначениеЗаполнено(UID) Тогда
	
		UID = Новый УникальныйИдентификатор;	
	
	КонецЕсли; 
	
КонецПроцедуры

Процедура ОбработкаПроведения(Отказ, РежимПроведения)
	
	ТаблицаДвижений = Документы.УстановкаЦенПредметовСнабжения.ПолучитьЗначенияРеквизитов(Ссылка);
	Движения.ЦеныПредметовСнабжения.Загрузить(ТаблицаДвижений);	
	Движения.ЦеныПредметовСнабжения.Записывать = Истина;
	
КонецПроцедуры

