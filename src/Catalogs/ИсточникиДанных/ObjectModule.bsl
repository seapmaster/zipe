
Процедура ПередЗаписью(Отказ)
	
	Если ТипСоединения = Перечисления.ТипыСоединенияИсточникаДанных.MSSQLConnection Тогда
		Представление = ИмяСервера + "/" + ИмяБазы;
	ИначеЕсли ТипСоединения = Перечисления.ТипыСоединенияИсточникаДанных.WebСервис Тогда
		Представление = СсылкаНаWebСервис;
	ИначеЕсли ТипСоединения = Перечисления.ТипыСоединенияИсточникаДанных.ЭлектроннаяПочта Тогда
		Представление = АдресПочты;
	КонецЕсли; 
	
КонецПроцедуры
