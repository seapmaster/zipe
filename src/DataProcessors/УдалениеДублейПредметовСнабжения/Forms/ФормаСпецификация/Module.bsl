
//////////////////////////////////////////////////////////////
// ОБРАБОТЧИКИ СОБЫТИЙ

&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	Если Параметры.Свойство("Оригинал", ПредметСнабжения) И Параметры.Свойство("АдресСпецификацииВХранилище") Тогда
		
		ИтоговаяСпецификация.Загрузить(ПолучитьИзВременногоХранилища(Параметры.АдресСпецификацииВХранилище));		
	
	КонецЕсли; 
	
КонецПроцедуры // ПриСозданииНаСервере
